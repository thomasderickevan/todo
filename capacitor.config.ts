import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thomasderickevan.todo',
  appName: 'Student Helper',
  webDir: 'dist'
};

export default config;
